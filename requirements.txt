# AutoSim 依赖清单 v0.1
# 目标：Python 3.10 / CUDA 12.1 / PyTorch 2.4 / 单机 H100 起步
# 注释里标 [git] 表示需要 git submodule 而非 pip。

# --- 基础 ---
numpy>=1.26
scipy>=1.11
opencv-python>=4.9
pillow>=10
pyyaml>=6
tqdm>=4.66
einops>=0.7
loguru>=0.7

# --- 深度学习 ---
torch>=2.4
torchvision>=0.19
# torch-scatter / torch-cluster 由 SparseDrive、DriveStudio 各自要求；按子模块文档安装

# --- 3DGS / 神经渲染 ---
gsplat>=1.3              # NuRec 后端，CARLA 0.9.16 集成依赖
nerfacc>=0.5
# DriveStudio: [git] github.com/ziyc/drivestudio
# 需要 COLMAP 二进制（系统包）

# --- 世界模型 ---
diffusers>=0.30
transformers>=4.45
accelerate>=0.34
# Cosmos-Predict2.5: [git] github.com/nvidia-cosmos/cosmos-predict2.5
# Vista (备份): [git] github.com/OpenDriveLab/Vista

# --- 闭环骨架 ---
# AlpaSim: [git] NVIDIA 待公开仓库
# OpenDriveLab SimScale: [git] github.com/OpenDriveLab/SimScale (备份)
# CARLA 0.9.16: 通过 docker 镜像 carla/carla:0.9.16

# --- E2E 算法 ---
# SparseDrive: [git] github.com/swc-17/SparseDrive (MVP)
# UniAD/VAD/DriveTransformer/Hydra-NeXt/DiffusionDrive: 各自仓库，Week 4+

# --- 评测 ---
nuscenes-devkit>=1.1.11
# Bench2Drive: [git] github.com/Thinklab-SJTU/Bench2Drive
# NAVSIM: [git] github.com/autonomousvision/navsim
# DriveArena: [git] github.com/PJLab-ADG/DriveArena
# HUGSIM: [git] github.com/hyzhou404/HUGSIM

# --- 数据集工具 ---
# DAIR-V2X devkit: [git] github.com/AIR-THU/DAIR-V2X
# ONCE devkit: [git] github.com/once-for-auto-driving/once_devkit

# --- 开发工具 ---
ruff>=0.6
pyright>=1.1.380
pytest>=8
pytest-cov>=5
ipykernel>=6
matplotlib>=3.8
