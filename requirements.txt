# AutoSim 依赖清单 v3 (2026-05-08)
# 目标：Python 3.10 / CUDA 12.1 / PyTorch 2.4 / 单机 H100 起步
# 注释里 [git] = 必须 git submodule，[docker] = docker 镜像，[pip] = pip 装

# --- 基础 ---
numpy>=1.26
scipy>=1.11
opencv-python>=4.9
pillow>=10
pyyaml>=6
tqdm>=4.66
einops>=0.7
loguru>=0.7

# --- 深度学习 ---
torch>=2.4
torchvision>=0.19

# --- 3DGS 渲染（v3 主路径）---
gsplat>=1.5.0           # ★ 主 renderer，支持 3DGUT 模式 (with_ut, with_eval3d, viewmats_rs)
nerfacc>=0.5
# DriveStudio + OmniRe: [git] github.com/ziyc/drivestudio  (训练管线)
# 备选/对照: SplatAD: [git] github.com/carlinds/splatad
# 闭环评测: HUGSIM: [git] github.com/hyzhou404/HUGSIM
# 不再使用: NuRec docker (闭源)、3DGRUT (仅 OptiX 反射时启用)

# --- CARLA 闭环骨架（v3 主路径）---
# CARLA 0.10.0: [docker] carlasim/carla:0.10.0  (默认)
# CARLA 0.9.16: [docker] carlasim/carla:0.9.16  (兜底，UE4)
# ScenarioRunner: [git] github.com/carla-simulator/scenario_runner
# OpenSCENARIO 2.0 解析: ScenarioRunner 内置 (ANTLR4)
carla>=0.10.0           # CARLA Python API (egg 文件，docker 内提供；本地开发可装 wheel)
# Scenic (可选 DSL): pip install scenic>=3.0

# --- gRPC bridge (v3 新增) ---
grpcio>=1.65
grpcio-tools>=1.65
protobuf>=5

# --- 世界模型 (alternative renderer，可选) ---
diffusers>=0.30
transformers>=4.45
accelerate>=0.34
# Cosmos-Predict2.5: [git] github.com/nvidia-cosmos/cosmos-predict2.5  (Apache + Open Model License)
# Vista (备份): [git] github.com/OpenDriveLab/Vista

# --- E2E 算法 ---
# SparseDrive: [git] github.com/swc-17/SparseDrive (MVP)
# UniAD/VAD/DriveTransformer/Hydra-NeXt/DiffusionDrive: 各自仓库，Week 4+
# mmcv / mmdet / mmdet3d 由各算法 README 自行约束（避免顶层 pin）

# --- 评测 ---
nuscenes-devkit>=1.1.11
# Bench2Drive: [git] github.com/Thinklab-SJTU/Bench2Drive (CARLA 闭环 220 条 case)
# NAVSIM: [git] github.com/autonomousvision/navsim (伪闭环)
# HUGSIM: [git] github.com/hyzhou404/HUGSIM (off-traj KID benchmark)

# --- 数据集工具 ---
# ONCE devkit: [git] github.com/once-for-auto-driving/once_devkit  (主)
# ApolloScape devkit: [git] github.com/ApolloScapeAuto/dataset-api  (兜底)
# DAIR-V2X: [git] github.com/AIR-THU/DAIR-V2X  (V2X 评测专用，不作重建)
# DrivingDojo: [git] github.com/Robertwyq/Drivingdojo  (Cosmos FT 数据，可选)

# --- 开发工具 ---
ruff>=0.6
pyright>=1.1.380
pytest>=8
pytest-cov>=5
ipykernel>=6
matplotlib>=3.8

# --- 已退出的依赖（不要再加回）---
# AlpaSim (NVlabs/alpasim) — 默认 renderer 闭源，第三方 planner 接入未文档
# WorldEngine (OpenDriveLab/WorldEngine) — 4s pseudo-closed-loop, 硬绑 nuPlan
# NuRec docker (nvcr.io/nvidia/nre/nre-ga) — 训练管线 + 推理服务全闭源
# SimScale 作为 agent 框架 — 误读，它是 sim-real 训练辅助
